<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bvd.android.financemanager">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".app.FinanceManagerApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AllExpensesActivity" />
        <activity android:name=".activities.DetailedExpenseActivity" />
        <activity android:name=".activities.MailActivity" />
        <activity android:name=".activities.AddExpenseActivity" />
        <activity android:name=".activities.DatePickerActivity" />
        <activity android:name=".activities.StatisticsActivity" />
        <activity android:name=".MainActivity" />
        <!--<activity
            android:name=".LoginActivity2"
            android:label="@string/title_activity_login2" />-->
        <activity android:name=".activities.login.RegisterActivity"></activity>

        <service
            android:name=".notifications.NotificationService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>